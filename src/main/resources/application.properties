spring.config.import=.env.properties, .api.properties
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

mybatis.type-aliases-package=com.zpop.web.entity, com.zpop.web.dto
mybatis.mapper-locations=classpath*:mapper/*Mapper.xml
logging.level.com.zpop.web.dao=trace
spring.output.ansi.enabled=always

#DB에 있는 데이터를 아래의 설정 없이 그냥 받아오면 UTC시간대로 표시됨 (현재시간 - 9 Hours)
spring.jackson.time-zone=Asia/Seoul

#server.error.include-stacktrace=never
