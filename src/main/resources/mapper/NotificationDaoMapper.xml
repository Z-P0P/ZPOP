<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zpop.web.dao.NotificationDao">
	 <select id="getList" resultType="com.zpop.web.entity.Notification">
		select * 
		from notification 
		where id = ${id}
	</select>
	 <select id="get" resultType="com.zpop.web.entity.Notification">
		select * from notification where id = ${id}
	</select>
	<update id="updateReadDate" parameterType="boolean">
		UPDATE notification SET readAt = '1999-07-19';
	</update>
</mapper>