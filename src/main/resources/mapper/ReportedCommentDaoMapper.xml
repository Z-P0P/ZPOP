<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zpop.web.dao.ReportedCommentDao">
	<insert id="insert" parameterType="ReportedComment">
		insert into reportedComment (commentId,reporterId,typeId,adminId,reason,original,createdAt,processedAt,blockedAt,releasedAt) 
		values(#{commentId},#{reporterId},#{typeId},#{adminId},#{reason},#{original},#{createdAt},#{processedAt},#{blockedAt},#{releasedAt});
	</insert>
</mapper>