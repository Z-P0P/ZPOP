<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zpop.web.dao.ParticipationDao">
    <insert id="insert" parameterType="com.zpop.web.entity.Participation">
        insert into participation
        (meetingId, participantId)
        values(#{meetingId}, #{participantId})
    </insert>

    <select id="get" resultType= "com.zpop.web.entity.Participation">
        select *
        from participation
        where id=#{id}
    </select>

    <select id="getListByMeetingId" resultType= "com.zpop.web.entity.Participation">
        select *
        from participation
        where meetingId=#{id}
    </select>

    <select id="getList" resultType= "com.zpop.web.entity.Participation">
        select *
        from participation
    </select>
   	<select id="getByMeetingId" resultType="MeetingParticipantsDto">
		SELECT p.id, p.participantId AS memberId, m.nickname, m.profileImagePath
		FROM participation p
		JOIN member m 
		ON p.participantId = m.id
		WHERE meetingId = #{meetingId} AND (p.bannedAt IS NULL AND p.canceledAt IS NULL);
    </select>
</mapper>
